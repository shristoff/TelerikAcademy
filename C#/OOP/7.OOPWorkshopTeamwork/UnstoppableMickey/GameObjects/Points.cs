namespace UnstoppableMickey
{
    using System;

    public class Points : TextObject
    {
        // Constructors
        public Points(Position position, string text, ConsoleColor color)
            : base(position, text, color)
        {
        }
    }
}
