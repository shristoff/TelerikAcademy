namespace UnstoppableMickey
{
    using System;

    public class CatsNumber : TextObject
    {
        // Constructors
        public CatsNumber(Position position, string text, ConsoleColor color)
            : base(position, text, color)
        {
        }
    }
}
