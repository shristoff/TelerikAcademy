using System;

    class Declare72InChar
{
    static void Main()
    {
        char symbol = '\u0048'; //Unicode symbol code in a hexadecimal format (letter 'H')
        Console.WriteLine(symbol);
    }
}
